[pytest]
markers =
    unit: unit tests
    integration: integration tests
    functional: functional tests
    regression: regression tests
    perf: perf/smoke tests
    pg: postgres-only integration tests
addopts = -q
